debug.env
.venv
venv
src/debug
__pycache__
download
supervisely
.python-version

# Kept out of the repository on purpose: the suite ships as a release artifact
# (tests.tar.gz) instead of being versioned here.
tests/
